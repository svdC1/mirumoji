"""
Contains the Mirumoji Launcher GUI implementation.

Modules are not imported eagerly here so that the base ``mirumoji`` install
(without the ``gui`` extra) doesn't attempt to load fastapi / flaskwebgui at
import time.
"""
